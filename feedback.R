## things to improve for paper

# theory and literature 
  # we need to investigate how aid is disbursed, the timing of aid disbursement,
  # what democracy characteristics donors can see and reward/punsh (what do they react to)

# methods
  # control for country fixed effects

  # look at VIF (variance inflation factor) -- this measures how much the variance of an estimated 
  # regression coefficient inflates if there is multicolinearity 
    # maybe try running the model without v2x_polyarchy as a control and compare the results

  # use as much data as possible, 10 year time period is arbitrary --> extend period of analysis

  # what are the differences between the various indicies in VDEM that measure democracy

